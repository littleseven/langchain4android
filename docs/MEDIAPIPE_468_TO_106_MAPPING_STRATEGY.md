# MediaPipe 468→106 点映射策略

## 概述

本文档定义 MediaPipe Face Landmarker（468点）到火山引擎 106 点标准的映射策略，**核心原则是确保所选点左右对称**。

## 核心映射原则

### 1. 第一性原理

**MediaPipe 468 点本身的拓扑结构不对称**，因此不能简单使用几何镜像（`x = 1-x`），而必须：

1. **先确定语义对称的固定点**：为每个 106 点找到 MediaPipe 中语义对应的固定点
2. **再插值过渡点**：在固定点之间使用 `midPoint` 插值
3. **验证左右对称性**：确保 M0-M15 与 M17-M32 关于 M16 对称

### 2. 左右对称定义

- **中轴点**：M16=152（下巴中心）
- **右半边**：M0-M15（画面左侧=被摄者右脸，从上到下）
- **左半边**：M17-M32（画面右侧=被摄者左脸，从下到上）
- **对称验证**：每个 M_i 与 M_(32-i) 应关于中轴线对称

### 3. MediaPipe 左右脸对称点对

基于 MediaPipe 468 点的几何坐标（归一化），以下点对关于 x=0.5 严格对称：

| 右脸点（画面左侧） | 左脸点（画面右侧） | 语义位置 |
|-------------------|-------------------|---------|
| 234 | 389 | 脸颊上/耳上 |
| 93 | 356 | 脸颊/耳下 |
| 132 | 454 | 脸颊/耳朵 |
| 58 | 323 | 脸颊/耳下 |
| 172 | 361 | 脸颊下/脸颊 |
| 136 | 288 | 下颌角/脸颊 |
| 150 | 397 | 下巴边缘/下颌角 |
| 149 | 365 | 下巴右侧/脸颊下 |
| 176 | 379 | 下巴中右/下巴边缘 |
| 148 | 378 | 下巴中心右侧/下巴中左 |
| 152 | 400 | 下巴/下巴左侧 |
| 377 | 377 | 下巴中心最低（自对称） |

> **验证**：234(x=0.71) ↔ 389(x=0.29)，0.71-0.5=0.21, 0.5-0.29=0.21 ✅

## 33 点轮廓映射策略

### 右半边 M0-M15（画面左侧，从上到下）

**策略**：使用 MediaPipe 右脸区域点，从外（鬓角）到中心（下巴）

| 106点 | MediaPipe点 | 语义位置 | 说明 |
|-------|------------|---------|------|
| M0 | 234 | 右脸颊上 | 固定点：右耳/脸颊上 |
| M1 | midPoint(234,93) | 右脸颊过渡 | 插值 |
| M2 | 93 | 右脸颊 | 固定点：右耳下 |
| M3 | 132 | 右脸颊 | 固定点：右脸颊上 |
| M4 | 58 | 右脸颊 | 固定点：右脸颊中 |
| M5 | 172 | 右脸颊下 | 固定点：右脸颊下 |
| M6 | 136 | 右下颌角 | 固定点：右下颌角 |
| M7 | 150 | 右下巴边缘 | 固定点：右下巴 |
| M8 | 149 | 下巴右侧 | 固定点：下巴右 |
| M9 | midPoint(149,176) | 下巴右过渡 | 插值 |
| M10 | 176 | 下巴中右 | 固定点：下巴中右 |
| M11 | midPoint(176,148) | 下巴中右过渡 | 插值 |
| M12 | 148 | 下巴中心右侧 | 固定点：下巴中心右 |
| M13 | midPoint(148,152) | 下巴中心过渡 | 插值 |
| M14 | 152 | 下巴 | 固定点：下巴中心 |
| M15 | midPoint(152,377) | 下巴中心过渡 | 插值 |

### 中轴点 M16

| 106点 | MediaPipe点 | 语义位置 |
|-------|------------|---------|
| M16 | 377 | 下巴中心最低（中轴点） |

### 左半边 M17-M32（画面右侧，从下到上）

**策略**：使用与右半边**语义对称**的 MediaPipe 左脸点，从中心（下巴）到外（鬓角）

| 106点 | MediaPipe点 | 对称右半边点 | 语义位置 | 说明 |
|-------|------------|-------------|---------|------|
| M17 | midPoint(377,400) | M15=midPoint(152,377) | 下巴中心左侧 | 插值 |
| M18 | 400 | M14=152 | 下巴左侧 | 固定点 |
| M19 | midPoint(400,378) | M13=midPoint(148,152) | 下巴左侧过渡 | 插值 |
| M20 | 378 | M12=148 | 下巴中左 | 固定点 |
| M21 | midPoint(378,379) | M11=midPoint(176,148) | 下巴中左过渡 | 插值 |
| M22 | 379 | M10=176 | 左下巴边缘 | 固定点 |
| M23 | 365 | M8=149 | 左脸颊下 | 固定点 |
| M24 | midPoint(365,397) | M7=150 | 左脸颊下过渡 | 插值 |
| M25 | 397 | M6=136 | 左下颌角 | 固定点 |
| M26 | 288 | M5=172 | 左脸颊 | 固定点 |
| M27 | midPoint(288,361) | M4=58 | 左脸颊过渡 | 插值 |
| M28 | 361 | M3=132 | 左脸颊上 | 固定点 |
| M29 | 323 | M2=93 | 左耳下 | 固定点 |
| M30 | midPoint(323,454) | M1=midPoint(234,93) | 左耳下过渡 | 插值 |
| M31 | 454 | M0=234 | 左耳上 | 固定点 |
| M32 | 389 | 无直接对应 | 左鬓角/轮廓终点 | 固定点 |

> **注意**：M32=389 与 M0=234 不完美对称（234↔389 是脸颊点，但 M0 是鬓角）。
> 由于 MediaPipe 左轮廓只有一个顶部点（389），而右轮廓有两个（127, 162），
> **妥协方案**：M32 使用 389，接受与 M0 不完全对称。

### 对称验证

| 右半边点 | 左半边对称点 | 是否对称 |
|---------|-------------|---------|
| M0=234 | M32=389 | ✅ 234↔389 |
| M1=midPoint(234,93) | M31=454 | ❌ 不对称（454↔132） |
| M2=93 | M30=midPoint(323,454) | ❌ 不对称 |
| M3=132 | M29=323 | ❌ 不对称（323↔58） |
| ... | ... | ❌ 不完全对称 |

**结论**：由于 MediaPipe 468 点的拓扑结构限制，**无法实现完美的 1:1 对称**。

## 最终策略选择

### 方案 A：语义优先（推荐）

**核心思想**：先确保每个点的语义正确，再尽量保持对称。

**优点**：
- 每个 106 点都有明确的 MediaPipe 语义对应
- 嘴部、鼻子、眼睛等核心区域映射准确
- 便于调试和维护

**缺点**：
- 左右轮廓不完全对称
- 顶部点（M0/M32）不对称

### 方案 B：强制几何对称

**核心思想**：右半边确定后，左半边使用几何镜像（`x = 1-x, y = y`）。

**优点**：
- 左右严格对称
- 无需手动指定左半边点

**缺点**：
- 左半边点可能没有语义对应（MediaPipe 中不存在）
- 可能导致某些区域点分布不均匀

### 方案 C：混合策略（实际采用）

**核心思想**：
1. **固定点语义映射**：关键语义点（下巴、脸颊、鼻翼等）使用 MediaPipe 语义对应点
2. **过渡点插值**：在固定点之间使用 `midPoint` 插值
3. **对称性妥协**：接受 MediaPipe 拓扑结构导致的不对称，但尽量靠近对称

**实施步骤**：
1. 确定右半边 M0-M15 的固定点（基于 MediaPipe 右脸区域）
2. 确定中轴点 M16=377
3. 为每个右半边固定点找到对应的左半边对称点
4. 在左半边使用对称点，并按相同规则插值
5. 验证左右对称性，记录不对称点

## 山根映射优化（基于 face001_landmarks.png）

**更新**：2026-04-23 根据 face001_landmarks.png 标注验证优化

### 旧映射（不准确）
- 78 = 281（画面左侧）
- 79 = 49（画面右侧）

### 新映射（优化后）
- 78 = 193（画面左侧，眉心左侧下方）
- 79 = 419（画面右侧，眉心右侧下方）

**验证依据**：
- 从 face001_landmarks.png 可以看到，193 和 419 位于山根两侧
- 281 和 49 位置偏外，不是山根的最佳选择
- 193↔419 关于眉心 168 对称

## 其他区域映射要点

### 眉毛映射

**右眉上部（33-37）**：
- 33=右眉头 → MediaPipe 296（右眉头）
- 34-36=右眉腰 → MediaPipe 297-299（右眉腰）
- 37=右眉尾 → MediaPipe 300（右眉尾）

**左眉上部（38-42）**：
- 38=左眉头 → MediaPipe 107（左眉头）
- 39-41=左眉腰 → MediaPipe 108-110（左眉腰）
- 42=左眉尾 → MediaPipe 111（左眉尾）

> **注意**：MediaPipe 眉毛点索引左右定义与 106 标准一致（画面左侧=右脸）。

### 嘴巴映射

**外轮廓（84-95）**：
- 84=左嘴角 → MediaPipe 61（左嘴角）
- 85-89=上唇 → MediaPipe 0,267,269,270,291（上唇从左到右）
- 90=右嘴角 → MediaPipe 291（右嘴角）
- 91-95=下唇 → MediaPipe 321,409,375,181,61（下唇从右到左）
- **93=下唇对称点** → MediaPipe 17（下唇中心）

**内轮廓（96-103）**：
- 96=左内角 → MediaPipe 78（上唇内左）
- 97-99=上唇内侧 → MediaPipe 88,0,87（上唇内侧从左到右）
- 100=右内角 → MediaPipe 308（下唇内左？需验证）
- 101-103=下唇内侧 → MediaPipe 324,310,308（下唇内侧从右到左）

## 实施注意事项

1. **前置摄像头镜像**：使用前置摄像头时，MediaPipe 返回的 x 坐标需要镜像：`x = 1 - x`

2. **归一化坐标**：MediaPipe 返回的坐标为归一化坐标（0-1），直接使用即可

3. **插值函数**：使用 `midPoint(a, b)` 计算两点中点

4. **调试方法**：
   - 在 CameraDebugOverlay 中显示 106 点（蓝色）
   - 对比 106 点与 MediaPipe 原始点的位置关系
   - 验证左右对称性

5. **验证清单**：
   - [ ] 下巴中心 M16 是否准确
   - [ ] 左右脸颊点是否对称
   - [ ] 嘴部 93/98/102 对称点是否准确
   - [ ] 眉毛左右是否对称
   - [ ] 眼睛左右是否对称

## 参考

- [VOLCANO_ENGINE_106_POINTS.md](./VOLCANO_ENGINE_106_POINTS.md)
- [MEDIAPIPE_468_POINTS.md](./MEDIAPIPE_468_POINTS.md)
- [BIG_BEAUTY_TECH_SPEC.md](./BIG_BEAUTY_TECH_SPEC.md)
